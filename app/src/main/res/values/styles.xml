<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>

    </style>

    <style name="textInputLabel" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/edtxtHintColor</item>
        <item name="android:textSize">@dimen/textSize14sp</item>
        <item name="colorPrimary">@color/edtxtHintColor</item>
        <item name="colorPrimaryDark">@color/edttxtColor</item>
        <item name="colorAccent">@color/edttxtColor</item>

        <item name="colorControlNormal">@color/edttxtColor</item>

        <item name="colorControlActivated">@color/edttxtColor</item>
    </style>

</resources>
